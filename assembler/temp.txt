Added modules LabelTable and VarTable to populate a (string, int) Hashtbl.t with the needed label names and their line numbers, and program variables and their RAM addresses, respectively. Added Modules Block and Program, which have encode functions, binding all previous functions into a small module. Added pretty print functionality to Vector module, and using the functionality in Block.encode and Program.encode functions. Changed the AST to have supplementary functions which aid in machine.ml
