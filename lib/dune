(library
 (name jackvm)
 (libraries assembler.lib)
 (modules vmast encode parser lexer)
)

(menhir (modules parser)
 (flags --exn-carries-state --trace --dump)
 ; (flags --exn-carries-state)

 ; (flags --strict)
)

(ocamllex (modules lexer))
